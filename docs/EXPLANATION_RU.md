# Как работает загрузка фото в посты и админ-панель
# Проект: SocialNet

## 📸 Загрузка фото в посты

### Как это работает (пошагово)

**1. Пользователь нажимает кнопку "добавить фото" при создании поста**

В файле `frontend/src/components/CreatePost.jsx` есть скрытый `<input type="file">` и кнопка с иконкой фото. Когда пользователь нажимает на кнопку — открывается окно выбора файла.

```
Кнопка фото → fileInputRef.current.click() → открывается окно выбора файла
```

**2. Проверка файла на фронтенде**

Файл `CreatePost.jsx` проверяет:
- Тип файла — только картинки (`file.type.startsWith('image/')`)
- Размер — не больше 5 МБ (`file.size > 5 * 1024 * 1024`)

Если файл не подходит — показывается ошибка.

**3. Отправка файла на сервер**

Файл отправляется через функцию `uploadAPI.uploadFile(file)` из `frontend/src/services/api.js`:

```javascript
uploadFile: (file) => {
    const formData = new FormData()
    formData.append('file', file)
    return api.post('/upload', formData, {
        headers: { 'Content-Type': 'multipart/form-data' },
    })
}
```

Файл отправляется как `multipart/form-data` на адрес `POST /api/upload`.

**4. Backend принимает файл**

В файле `internal/http/router.go` есть функция `handleUpload()`, которая:

1. Проверяет, что метод = POST
2. Парсит multipart-форму (максимум 10 МБ)
3. Получает файл из поля `"file"`
4. Проверяет расширение — разрешены только: `.jpg`, `.jpeg`, `.png`, `.gif`, `.webp`
5. Генерирует уникальное имя файла: `20260212143555_photo.jpg` (дата + оригинальное имя)
6. Сохраняет файл в папку `./uploads/`
7. Возвращает JSON: `{"url": "/uploads/20260212143555_photo.jpg"}`

**5. Frontend получает URL и привязывает к посту**

После загрузки, URL картинки сохраняется в state `mediaUrl`. Когда пользователь нажимает "Post", отправляется запрос на создание поста:

```javascript
postsAPI.create({ content: "текст поста", media_url: "/uploads/20260212143555_photo.jpg" })
```

**6. Backend сохраняет пост в базу данных**

В файле `internal/http/handler/post_handler.go` функция `CreatePost()`:
- Получает JSON с полями `content` и `media_url`
- Вызывает `postService.CreatePost()` — сохраняет в SQLite

Модель поста (`internal/model/post.go`):
```go
type Post struct {
    ID        int64     `json:"id"`
    UserID    int64     `json:"user_id"`
    Content   string    `json:"content"`
    MediaURL  string    `json:"media_url,omitempty"`  // ← URL картинки
    CreatedAt time.Time `json:"created_at"`
    ...
}
```

**7. Отображение картинки**

При показе ленты, картинка загружается через тег `<img src={post.media_url}>`. 
Backend раздаёт статические файлы из папки `./uploads/` по адресу `/uploads/...`.

### Схема потока данных

```
Пользователь выбирает фото
        ↓
[Frontend] Проверка (тип, размер)
        ↓
POST /api/upload (multipart/form-data)
        ↓
[Backend] Проверка расширения → Сохранение в ./uploads/
        ↓
Ответ: {"url": "/uploads/filename.jpg"}
        ↓
[Frontend] Сохраняет URL в state
        ↓
POST /api/posts (JSON: {content, media_url})
        ↓
[Backend] Сохраняет пост с URL картинки в SQLite
        ↓
При показе ленты: <img src="/uploads/filename.jpg">
```

### Ключевые файлы загрузки фото

| Файл | Роль |
|------|------|
| `frontend/src/components/CreatePost.jsx` | Форма создания поста + загрузка фото |
| `frontend/src/services/api.js` | Функция `uploadAPI.uploadFile()` |
| `internal/http/router.go` | Функция `handleUpload()` + раздача файлов |
| `internal/model/post.go` | Модель поста с полем `media_url` |
| `internal/config/config.go` | Настройка `UPLOAD_DIR` (по умолчанию `./uploads`) |

---

## 🛡️ Админ-панель

### Что может делать админ

Админ-панель находится на странице `/admin` и доступна только пользователям с правами администратора.

### Вкладки админ-панели

| Вкладка | Что делает |
|---------|-----------|
| 📊 **Статистика** | Показывает общее кол-во: пользователей, онлайн, постов, групп, сообщений, жалоб |
| 📋 **Жалобы** | Просмотр и обработка жалоб (pending/reviewed/resolved). Можно удалить контент |
| 😊 **Эмодзи** | Статистика какие эмодзи-аватары используют пользователи. Можно отправить рассылку по группе эмодзи |
| 📢 **Рассылка** | Массовая рассылка уведомлений всем пользователям + история рассылок |
| 🛡️ **Админы** | Поиск пользователей и назначение прав администратора |
| 👥 **Группы** | Создание новых групп (название + описание), просмотр существующих |

### Как работает backend админ-панели

Все админские API-эндпоинты защищены middleware `RequireAdmin`:

```
/admin/stats         → GET    → Статистика сайта
/admin/grant         → POST   → Дать права админа (user_id)
/admin/broadcast     → POST   → Массовая рассылка
/admin/broadcast     → GET    → История рассылок
/admin/broadcast/emoji/:id → POST → Рассылка по эмодзи
/admin/delete/:type/:id   → DELETE → Удалить контент
/reports             → GET    → Получить жалобы (с фильтром)
/reports/:id         → PUT    → Обработать жалобу
```

Основной handler: `internal/http/handler/admin_handler.go`
Маршрутизация: `internal/http/router.go` (строки 238-265)

### Ключевые файлы админ-панели

| Файл | Роль |
|------|------|
| `frontend/src/pages/Admin.jsx` | React-компонент админ-панели (UI) |
| `frontend/src/pages/Admin.css` | Стили админ-панели (сайдбар, карточки) |
| `frontend/src/services/api.js` | `adminAPI` — все API-вызовы админа |
| `internal/http/handler/admin_handler.go` | Go-хэндлеры для админских API |
| `internal/http/router.go` | Маршруты + middleware авторизации |
| `internal/http/middleware/` | `RequireAdmin` — проверка прав |

---

## 🔧 Конфигурация

Настройки в файле `internal/config/config.go` через переменные окружения:

| Переменная | По умолчанию | Описание |
|-----------|-------------|----------|
| `UPLOAD_DIR` | `./uploads` | Папка для загруженных файлов |
| `MAX_UPLOAD_SIZE` | 10 МБ | Максимальный размер файла |
| `SERVER_PORT` | 8080 | Порт Go-сервера |
| `JWT_SECRET` | (встроенный) | Секрет для JWT-токенов |
| `INITIAL_ADMINS` | (нет) | Email первых админов через запятую |
